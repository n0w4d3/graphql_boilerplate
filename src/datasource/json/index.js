const User = require('./users.json')
const Todo = require('./todos.json')
const Album = require('./albums.json')
const Photo = require('./photos.json')
const Post = require('./posts.json')
const Comment = require('./comments.json')



module.exports = {
   User, Todo, Album, Photo, Post, Comment,
}